//! Core substrate logic shared across the tool: shared types and the
//! DTCG-token → CSS-custom-properties compiler. This module is agent- and
//! server-agnostic; it only knows about the files-as-truth substrate.

pub mod tokens;

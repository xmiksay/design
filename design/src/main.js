// Page entry — register every component. The preview HTML supplies the content;
// this module just pulls in the barrel so the custom elements upgrade.
import "../components/index.js";
import "./theme.js";
